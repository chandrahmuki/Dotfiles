#!/bin/sh
# distrobox_binary
# name: archbox
if [ -z "${CONTAINER_ID}" ]; then
	exec "/usr/bin/distrobox-enter"  -n archbox  --  '/usr/bin/pistol'  "$@"
elif [ -n "${CONTAINER_ID}" ] && [ "${CONTAINER_ID}" != "archbox" ]; then
	exec distrobox-host-exec '/home/david/.local/bin/pistol' "$@"
else
	exec '/usr/bin/pistol' "$@"
fi
